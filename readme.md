Åsna means donkey in Swedish :)

This service is a good vehicle for modeling realistic asynchronous job-processing system: async I/O at the network edge, bounded coordination inside the runtime, and CPU-bound work offloaded to worker threads.
